<div id="container">

  <div id="headings-and-form">
    <div id="headings">
      <span id="burgers-and-title">
        <img class="burger-jawn" src="/assets/css/burger.png" alt="a burger">
        <h1>CRUD Burger POS System lite</h1>
        <img class="burger-jawn" src="/assets/css/burger.png" alt="a burger">
      </span>
      <h2>Because so many things will be misheard before they are misread!</h2>

      <h2 class="with-bg-color">New Order</h2>
      <h4>Process incoming orders here:</h4>
    </div>

    <form class="create-form">

      <div class="form-group">
        <label for="burg">Burger Name:</label>
        <input type="text" id="burg" name="name">
      </div>

      <button id="submit-button" type="submit">Add Burger</button>
    </form>

  </div>

  <div id="orders-placed">
    <h2 class="with-bg-color">Orders Placed</h2>
    <h4>Cooking staff: When orders are ready, click "MADE!"</h4>
    <ul>
      {{#each burgers}}
      {{#unless made}}
      {{> burgers/burger-block made=true}}
      {{/unless}}
      {{/each}}
    </ul>
  </div>

  <div id="orders-up">
    <h2 class="with-bg-color">Orders Up</h2>
    <h4>Cooking staff: If hit "MADE!" by mistake, click "NOT MADE?"</h4>
    <ul>
      {{#each burgers}}
      {{#if made}}
      {{> burgers/burger-block made=false}}
      {{/if}}
      {{/each}}
    </ul>
  </div>
</div>